fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
email-validator==2.2.0
neo4j==5.27.0
pandas==2.2.3
rapidfuzz==3.11.0
anthropic==0.42.0
openai==1.58.1
python-dotenv==1.0.1
python-multipart==0.0.20

# --- GNN inference (app/engine/algorithmic/gnn_recommendation_agent.py) ---
# Same versions as ml/requirements.txt -- the backend loads the checkpoint
# ml/train_gnn.py produces and must use a matching torch/PyG build to
# deserialize it. GNNRecommendationAgent still degrades gracefully (never
# raises) if these ever go missing/mismatched in another deployment target.
torch==2.13.0
torch_geometric==2.8.0
scikit-learn==1.9.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
faker==33.1.0
